#include <windows.h>
#include <stdio.h>

using namespace Windows::UI::Input::Preview::Injection;

//int
//main(void)
int main(void)//Platform::Array<Platform::String^>^ args)
{
	InjectedInputPenInfo^ a = ref new InjectedInputPenInfo();
	//auto _inputInjector InputInjector(); //.TryCreate();
	//InputInjector _inputInjector;
	//InjectedInputVisualizationMode visualMode;
	//InitializePenInjection(visualMode);
	return(0);
}